#include <algorithm>
#include <cmath>
#include <iostream>
#include <vector>

int fibonacci(int n);
std::vector<int> *criaVector();
void leVector(std::vector<int> &v);
void ordenaVector(std::vector<int> &v);
void printVector(const std::vector<int> &v);
void liberaVector(std::vector<int> *v);
double raizQuadrada(double n);